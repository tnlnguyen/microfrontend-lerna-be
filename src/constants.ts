export enum ChatEvent {
  CONNECT = 'connect',
  DISCONNECT = 'disconnect',
  MESSAGE = 'message'
}

export enum SnippetEvent {
  CONNECT = 'connect',
  CODE_CHANGED = 'CODE_CHANGED',
  MESSAGE = 'message' 
}
